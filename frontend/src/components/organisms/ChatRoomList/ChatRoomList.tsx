import ChatRoomListView, { ChatRoomListViewProps } from './ChatRoomList.view';

const ChatRoomList = (props: ChatRoomListViewProps) => {
    return <ChatRoomListView {...props} />;
};

export default ChatRoomList;
