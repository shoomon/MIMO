import ParticipantNameView, {
    ParticipantNameProps,
} from './ParticipantName.view';

const ParticipantName = (props: ParticipantNameProps) => {
    return <ParticipantNameView {...props} />;
};

export default ParticipantName;
